# TODO: 
	- Cover with unit tests!

# NOTES:
	- Publishing new version command: 
		- dotnet nuget push <nuget package file path> --api-key <api key> --source https://api.nuget.org/v3/index.json
		- <nuget package file path> would be e.g. FCsToolkit.1.0.4-alpha.nupkg